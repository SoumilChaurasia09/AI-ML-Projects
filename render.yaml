services:
  - type: web
    name: cancer-detection-dashboard
    env: python
    plan: free
    rootDir: cancer_detection
    buildCommand: pip install -r requirements.txt && python preload_dataset.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
